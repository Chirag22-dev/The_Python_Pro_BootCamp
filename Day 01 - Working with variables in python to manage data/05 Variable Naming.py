""" Variable names must start with a letter or underscore, contain only letters, numbers,
 and underscores, and must not be Python keywords."""

# Ask the user to enter their username and store it in the variable 'user_name'
user_name = input("Enter your username: ")

# Calculate the number of characters in the username
length = len(user_name)

# Print the username entered by the user
print(user_name)

# Print the length of the username
print(length)
